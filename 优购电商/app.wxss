/* 微信小程序不支持通配符* */
page,view,text,navigator,swiper,swiper-item{
    padding: 0;
    margin: 0;
    box-sizing: border-box;
}
/* 主题颜色用到的地方较多，可以定义一个主题颜色 */
page{
    /* 定义主题颜色 */
    --themeColor:#eb4450;
    /* 定义统一字体大小 */
    font-size:28rpx;
}

/* 使全局的图片宽度都是充满父容器 */
image{
    width: 100%;
}